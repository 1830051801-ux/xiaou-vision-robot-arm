# 小U离线验证总报告

日期：2026-08-09

范围：第 2-7 项离线验证。所有步骤保持 `motion_enabled=false`，不打开串口、CAN、真实 ROS2/MoveIt 硬件节点，也不发送真实运动命令。

## 2. MuJoCo 物理抓取回归

命令：

```powershell
& D:\机械臂\.venv_transformer_gpu\Scripts\python.exe tools/run_randomized_physical_regression.py `
  --seeds 20260809,20260810,20260811 `
  --trials-per-scenario 6 `
  --max-sampling-attempts 300 `
  --geometry-jitter-pct 0.05 `
  --output runtime/simulations/randomized_physical_regression_detailed_20260809.json
```

结果：

- 3 个独立种子，6 个场景，每场景 6 次，共 108/108 通过。
- 场景：`bottle`、`cup`、`pen`、`desktop_item`、`tissue_pull`、`cola`。
- 检查：POE/IK 收敛、关节限位、五次轨迹、桌面碰撞、自碰撞、抓取接触、抬升、释放后桌面接触。
- 每个场景 `failure_counts={}`、`warning_counts={}`；硬件运动、串口、CAN、ROS 硬件启动标志均为 false。
- 随机化范围：`x=0.235..0.315 m`、`y=-0.155..-0.085 m`、偏航 `-3..3 deg`、尺寸/质量抖动 `+/-5%`。
- 最大采样尝试次数：瓶子 54、水杯 27、笔 31、桌面物 67、纸巾 86、可乐 50。

限制：纸巾是刚性叠层/抽取代理，不是可变形布料；物体尺寸、摩擦、关节限位和抓取力仍是离线假设。

## 3. YOLO 数据、训练和 CPU ONNX

检查命令：

```powershell
& D:\机械臂\.venv_transformer_gpu\Scripts\python.exe tools/validate_yolo_dataset.py `
  --yaml runtime/yolo_train/xiaou_objects_dataset_deep/dataset.yaml `
  --expected-classes pen,bottle,cola,earphone --require-classes
```

四个数据集副本（source、deep、augmented、augmented_conservative）全部通过：

- 100 张图，292 个实例；`pen/bottle/cola/earphone` 四类均覆盖。
- train 80 张/233 个实例，val 20 张/59 个实例。
- 缺失标签 0、孤立标签 0、不可读图片 0、解析错误 0。
- 清除了过期 source 副本 `runtime/yolo_train/xiaou_objects_dataset/labels/train/pen_calib_028.txt` 的零宽框；检查器仍会把零宽/零高框作为硬错误。

GPU 训练轨迹（`results.csv` 最佳行）：

| 模型 | epoch | mAP50 | mAP50-95 |
|---|---:|---:|---:|
| deep 生产候选 | 117/120 | 0.97009 | 0.52897 |
| 强增强 | 110/120 | 0.90308 | 0.40550 |
| 保守增强 | 85/104 | 0.96539 | 0.49863 |

当前仍使用 deep 生产候选；训练数据没有真实 `cup` 或 `tissue` 标注，不能宣称模型已学会水杯或纸巾。

适配器检查：

- 单 deep ONNX：3 张验证图输出 11 个检测，`opencv-onnx-cpu` 后端正常。
- deep + 旧模型 union：3 张验证图输出 14 个检测，两套模型都能加载并返回统一的类别、置信度、框和中心点。

CPU ONNX 基准（单线程，预热 10 次，每图 30 次，共 90 次）：

| 模型 | 大小 | mean | p50 | p95 | 输出类别 |
|---|---:|---:|---:|---:|---|
| deep | 12,240,890 B | 172.35 ms | 160.97 ms | 228.22 ms | 四类 |
| conservative | 12,240,908 B | 160.20 ms | 155.74 ms | 189.05 ms | 四类 |

Pi 实际 RSS、温度和帧率尚未测量。

## 4. Transformer 训练、量化和回放

训练命令使用 CUDA、2048 条合成轨迹、32 epoch、序列长度 8、batch 64。

结果：

- CUDA 可用，训练集 1638、验证集 410。
- 最终验证策略准确率 1.0、阶段准确率 1.0、动作 MAE 0.0154779。
- FP32 ONNX + INT8 ONNX 导出通过；量化为动态 INT8 Linear 权重。
- 六个场景策略匹配 6/6、阶段匹配 6/6、状态匹配 6/6。
- 最大动作参数绝对差约 0.01043，最大风险差约 0.00066。
- FP32 可部署包（含 `.onnx.data`）约 1,918,855 B；INT8 约 700,064 B；比例约 0.3648。
- PyTorch CPU 回放和 ONNX Runtime CPU 回放均保存了独立 JSON。

限制：训练标签来自可解释仿真规则，只能证明模型结构、导出和接口，不代表真实抓取成功率。

## 5. ROS2、TF、话题和安全门

结果：

- `tools/verify_ros2_architecture.py`：架构审计通过，失败 0。
- `ros2_ws/src` Python 源码 compileall 通过。
- 六个场景全部验证 `base_link -> sim_*` TF、目标 Pose、46 维观测、策略输出和状态消息。
- 话题契约覆盖 `/xiaou/target_pose`、`/xiaou/target_status`、`/xiaou/decision_observation`、`/xiaou/decision`、`/xiaou/hardware_ready` 及仿真话题。
- 源码门确认 MoveIt `allow_trajectory_execution=false`、planner `allow_execution=false`，且必须经过 `/xiaou/hardware_ready`。
- 安全门回放中所有场景均被明确阻断，原因包含 `motion_enabled`、`hardware_ready`、`feedback_verified`。
- `ros2_process_started=false`、`ros2_cli_required=false`；当前 Windows 没有 `ros2`/`colcon`，未做真实 ROS2 进程启动。

## 6. UART、CAN 和 J5 新鲜度

结果：

- UART `CMD_GET_JOINT` 序列 31 被拆成 10 个分片，J5 payload 正确重组。
- CRC 篡改被拒绝；坏转义、帧边界、固定六轴状态长度和动作锁定响应的专项测试通过。
- J5 时间年龄 2.25 s、阈值 0.50 s，被判为 stale；J1-J4/J6 保持在线候选。
- CAN 位置命令、反馈、诊断帧往返通过；错误节点 ID 被拒绝。
- CAN 被动监听测试确认源代码没有发送调用，并保留错误/扩展标志。
- UART 专项测试 12 个通过；CAN/被动监听专项测试 10 个通过。
- 动作门缺少 motion、协议、F407、急停、反馈、校准、限位、速度等实测字段，保持 locked。

## 7. 树莓派部署包

新生成独立包：`runtime/deployment/xiaou_pi_inference_fullverify_20260809.tar.gz`

- 32 个文件，归档 10,987,770 B，未压缩 payload 13,076,583 B。
- SHA-256：`b1b736c7396ee1adce28fe371952be73c24cf42800f13d6b9e422aad5bc92c15`。
- 只包含 ONNX 推理和安全/协议代码；无 `.pt`、PyTorch、Ultralytics、MuJoCo、桌面数据集或相机采集内容。
- 主机导入探针：`cv2=true`、`numpy=true`、`onnxruntime=true`。
- 解包烟测：YOLO 四类检测 4 个，Transformer INT8 元数据和推理通过。
- 启动脚本 `bash -n` 返回 0；实际 Pi shell、ROS2、依赖安装仍待设备验证。
- 2GB 预算：预留 OS/ROS2 512 MB，估算进程 768 MB，总计 1280 MB，预算通过并保留 256 MB 余量。

## 总结

本轮离线 2-7 项全部通过；代码全套 `46 passed`，全工程 compileall 通过，最终硬件预检通过。可以继续优化仿真、视觉、策略和界面，但不能把这些结果当成真实电机在线、真实 ROS2 启动或真实抓取成功率。

硬件回来后的第一步仍是运行 `tools/hardware_return_preflight.py`，然后按 UART 只读、CAN 被动监听、急停/标定、单轴低速和实物回归顺序推进。
