# 现场验证记录（2026-09-22）

## 记录性质

本条目记录项目操作者在真实机械臂现场给出的能力确认。它不是本机重新采集的串口日志、视频或统计实验，也不替代设备级验收报告。当前提交没有收到原始日志、视频、设备型号、固件哈希、标定版本、执行次数或失败计数，因此只保留定性结论，不补写数值指标。

## 操作者确认的结果

| 能力 | 结果 | 证据等级 |
| --- | --- | --- |
| 真实机械臂运动 | 成功 | `operator_reported` |
| 桌面物品抓取 | 成功 | `operator_reported` |
| 桌面整理 | 成功 | `operator_reported` |
| 垃圾清理 | 成功 | `operator_reported` |
| 预设功能 | 操作者确认已完备 | `operator_reported` |

这里的“成功”表示操作者在当前设备、当前配置和当前现场观察到目标动作完成。它不推导成功率、重复性、周期时间、负载范围、碰撞裕量或长期可靠性。

## 与仓库中其他证据的关系

- `docs/evidence/consolidation_20260920/`、`consolidation_20260922/` 和 `offline_validation_20260820/` 仍然保留为离线仿真、协议回放和软件验证证据。
- 离线策略回放中的失败结果不会被删除，也不会因为本次现场反馈而改写成真实训练指标。
- 本记录不改变公开仓库测试和示例的默认运动锁，也不把示教参数复制成通用标定值。
- 后续若补充原始串口日志、视频、设备/固件标识和重复试验记录，应在本目录新增带时间戳的附件，并将本条目升级为可审计的现场验收记录。

机器可读摘要见 [`field_validation.json`](field_validation.json)。
