# 桌面机械臂助手功能清单

这个项目先让树莓派独立完成视觉、语音输入准备、联网 AI 对话和任务指令输出。STM32 和电机后面接好以后，再由树莓派把 JSON 指令发给 STM32。

## 最终展示建议做 6 个核心功能

| 功能 | 当前实现方式 | 说明 |
| --- | --- | --- |
| 语音/文字控制 | 先用键盘输入，后面接麦克风语音转文字 | 命令例子：帮我拿水杯、整理桌面、停止、回零 |
| 视觉识别 | YOLOv8n 摄像头识别 | 默认识别 cup、bottle、cell phone、book、keyboard、mouse、scissors |
| 机械臂取物 | 树莓派输出目标物体中心坐标 | 先输出 JSON，后面通过串口发 STM32 |
| 桌面整理 | 输出 tidy 指令 | 后面可以让 STM32 执行预设整理动作 |
| 安全停止 | 输出 stop 指令 | 后面接急停或软件停止 |
| AI 对话 | 云端大模型 API 或本地关键词兜底 | 没填 API Key 也能先跑基本命令 |

## 当前程序能直接做什么

```text
1. 测树莓派系统环境
2. 测摄像头
3. 用 YOLO 识别水杯、瓶子、手机、书本等
4. 输入“帮我拿水杯”，解析成 pick cup
5. 摄像头找到水杯后输出 x/y 坐标
6. 后面 STM32 接好后，可以把 JSON 指令发串口
```

## 当前先不做什么

```text
1. 不直接控制电机
2. 不直接做机械臂逆解
3. 不依赖 STM32 才能运行
4. 不一开始就做完整语音识别，先把视觉和 AI 流程跑通
```

## 推荐演示话术

```text
用户输入：帮我拿水杯
树莓派：解析意图 pick cup
摄像头：YOLO 定位水杯中心坐标
程序输出：{"cmd":"pick","object":"cup","x":320,"y":210}
后续扩展：STM32 根据坐标执行机械臂抓取
```
